Flask==3.1.3
gunicorn==22.0.0
sqlalchemy==2.0.49
psycopg[binary]==3.2.10
pydantic==2.12.5
stripe==12.5.1
# Security pins — transitive deps com CVE: bump explícito para versão patched.
# requests<2.33.0  → GHSA-gc5v-m9x4-r6x2 (tempfile race condition em extract_zipped_paths)
# urllib3<2.7.0    → PYSEC-2026-141 (cross-origin redirect leaking auth header)
#                  → PYSEC-2026-142 (Brotli decompression DoS)
requests>=2.33.0
urllib3>=2.7.0

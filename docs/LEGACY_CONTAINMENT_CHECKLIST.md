# Legacy Containment Checklist

- `/training` renders Ascension Duel.
- `/training-legacy` renders the retired Arena.
- Public CTAs point to `/training`, `/collection-ascension` and `/deck-builder-ascension`.
- The old Arena files remain available for fallback.
- New Ascension pages do not expose old type labels as primary product language.
- Legacy code may retain old terminology internally while migration continues.
